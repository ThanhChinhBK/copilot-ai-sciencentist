# Experiment Log: <Idea Title>

- **Idea:** `ideas/<topic>.idea.md`
- **Started:** <YYYY-MM-DD HH:MM>

## Results Summary

| Approach | Status | Key Metric | Notes |
|---|---|---|---|
| e.g. baseline-linear-probe | executed / abandoned / not executed | e.g. acc=0.71 | |

## Candidate Approaches (ranked)
1. **<Approach A>** — expected informativeness vs. cost rationale.
2. **<Approach B>** — ...
3. **<Approach C>** — ...

## Node Log

### Node 1: <Approach name>
- **Plan:** what this approach tests and how.
- **Code:** path/link to the code written for this node.
- **Command run:** exact command executed via the sandbox/CI.
- **Output:** relevant captured output (trimmed, but not fabricated).
- **Interpretation:** what this result means for the hypothesis.
- **Decision:** keep / abandon (reason) / promote to next round.

### Node 2: <Approach name>
(repeat structure above; add nodes as needed, bounded to a small number of debug
attempts per node)

## Debug Attempts (if any)
- Attempt 1: error → fix tried → outcome.
- Attempt 2: ...
(stop after 3 attempts per node; mark node abandoned if still failing)

## Conclusion
Was the hypothesis supported, refuted, or inconclusive, based only on executed nodes?
